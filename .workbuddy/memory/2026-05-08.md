# 2026-05-08

## 新增数据日志录制 & 回放

新增全局数据录制 & 回放能力，所有 DataBus 通道的数据均可录制到 .estlog 文件并离线回放。

### 新增文件 (3个)
- `src/core/services/DataLogger.h/.cpp` — DataLogger 录制引擎(JSONL格式)+ DataPlayer 回放引擎(变速/播放/暂停/seek)
- `src/app/widgets/PlaybackControl.h/.cpp` — 回放控制浮动栏(播放/暂停/停止/进度条/变速)

### 修改文件 (4个)
- `src/core/CMakeLists.txt` — 添加 DataLogger
- `src/app/CMakeLists.txt` — 添加 PlaybackControl
- `src/app/AppShell.h/.cpp` — 集成录制按钮(状态栏) + 回放控制(DockWidget底部) + DataBus hook + 回放发布

### 功能特性
- 录制：状态栏红色 REC 按钮点击开始/停止，自动生成 logs/est_yyyyMMdd_HHmmss.estlog
- .estlog 格式：每行 JSON，含 ts/ch/payload/meta 字段
- 回放：底部浮动控制栏，◀⏸▶ 控制 + 进度拖拽 + 0.5x~10x 变速
- 回放时自动通过 DataBus 发布，所有页面(波形图/协议解析/RTOS/CAN)自动响应
- 支持录制进度实时显示 (时间 + 文件大小)

## 新增 CAN 总线 (CANable) 页面

### 修改文件
- `src/resources/resources.qrc` — 添加 est_serial_helper.py
- `src/app/CMakeLists.txt` — 添加 TerminalWidget + SerialConsolePage
- `src/app/AppShell.h/.cpp` — 注册页面、侧边栏、首页连接
- `src/app/pages/HomePage.h/.cpp` — 添加终端控制台按钮(第3行)
- `src/app/widgets/ModuleIconFactory.cpp` — 添加 terminal 图标(终端屏幕)

### 功能特性
- 终端：深色主题、ANSI彩色支持(前景/背景/粗体)、命令历史、分离输入行
- 脚本：CodeEditor编辑Python、QProcess子进程执行、JSON协议通信
- 脚本API：send()/send_hex()/wait()/print()/delay()
- 支持自动测试脚本(AT指令等)

## 新增协议解析器 (Protocol Decoder)

新增二进制协议解析器功能，支持自定义模板、帧头匹配、字段解码和校验和验证。

### 新增文件 (8个)
- `src/core/services/ProtocolTemplate.h/.cpp` — 数据结构(FieldDef/ProtocolTemplate) + JSON序列化 + QSettings持久化
- `src/core/services/ProtocolDecoder.h/.cpp` — 解析引擎：帧头匹配、长度字段读取、逐字段解码(HEX/DEC/Signed/Float/ASCII/BIN)、校验和(XOR/Sum/CRC8)
- `src/app/pages/ProtocolDecoderPage.h/.cpp` — 页面：工具栏(模板管理/暂停/清空) + 帧列表(动态列) + 帧详情(字段级分解) + DataBus订阅
- `src/app/widgets/ProtocolTemplateEditor.h/.cpp` — 模板编辑对话框：名称/字节序/校验算法/字段表格(7列)/添加删除上移下移

### 修改文件
- `src/core/CMakeLists.txt` — 添加 ProtocolTemplate + ProtocolDecoder
- `src/app/CMakeLists.txt` — 添加页面 + 编辑器
- `src/app/AppShell.h/.cpp` — 注册页面、侧边栏(指南上方)、首页连接
- `src/app/pages/HomePage.h/.cpp` — 添加协议解析按钮(第2行第3列)
- `src/app/widgets/ModuleIconFactory.cpp` — 添加 protocol 图标(数据包+字段条+放大镜)

### 功能特性
- 自定义协议模板(帧头+长度+命令+负载+校验+任意字段)
- 帧头自动匹配、长度字段驱动帧长计算
- 6种显示格式：HEX/DEC/有符号/BIN/ASCII/FLOAT
- 3种校验算法：XOR/累加和/CRC8
- 帧列表动态列(按字段自动扩展) + 选中帧详情面板
- 帧列表上限500条，自动清除最早数据

## 新增用户手册页面 (User Guide)

新增完整的用户手册页面，涵盖全部 6 个功能模块的详细使用说明。

### 新增文件
- `src/app/pages/UserGuidePage.h/.cpp` — 页面：搜索工具栏 + QTextBrowser(从QRC加载HTML)
- `src/resources/docs/user_guide.html` — 完整手册内容（6章节+目录+搜索高亮）

### 修改文件
- `src/resources/resources.qrc` — 添加 docs/user_guide.html
- `src/app/CMakeLists.txt` — 添加 UserGuidePage.cpp/.h
- `src/app/AppShell.h/.cpp` — 注册 UserGuidePage，侧边栏项，首页连接
- `src/app/pages/HomePage.h/.cpp` — 添加使用手册按钮
- `src/app/widgets/ModuleIconFactory.cpp` — 添加 guide 图标(书本)

### 手册内容
6 个章节：串口工具 / 数据转换 / BIN 文件分析 / 文件比较 / 波形图 / RTOS 监控
每章节包含：功能概述 + 操作步骤 + 工具栏说明 + 使用技巧

## 新增 FreeRTOS / RTOS 任务监视器

新增 RTOS 任务监视器页面，通过串口接收 vTaskList() 和 vTaskGetRunTimeStats() 输出，实时解析并以表格/进度条可视化任务状态。

### 新增文件
- `src/core/services/RtosTaskParser.h/.cpp` — 核心解析服务：数据结构(RtosTaskInfo/RtosCpuInfo/RtosSnapshot) + 解析方法(parseTaskList/parseRuntimeStats/parseSnapshot) + 工具方法
- `src/app/pages/RtosMonitorPage.h/.cpp` — 页面：工具栏(格式选择/暂停/清空/立即解析/配置总栈) + QTableWidget 任务表格 + CpuBarWidget CPU概览 + DataBus订阅 + QSettings持久化栈配置

### 修改文件
- `src/core/CMakeLists.txt` — 添加 RtosTaskParser.cpp/.h
- `src/app/CMakeLists.txt` — 添加 RtosMonitorPage.cpp/.h
- `src/app/AppShell.h/.cpp` — 注册 RtosMonitorPage、侧边栏项、首页按钮连接
- `src/app/pages/HomePage.h/.cpp` — 添加 RTOS 监控按钮和信号
- `src/app/widgets/ModuleIconFactory.cpp` — 添加 rtos 图标(芯片+列表线条)

### 功能特性
- 自动检测 vTaskList 和 vTaskGetRunTimeStats 格式
- 手动/自动解析两种触发模式，支持从剪贴板粘贴数据
- 7列任务表格：编号/名称/状态(彩色)/优先级/栈余量/栈使用% (QProgressBar)/CPU%
- CPU 占比概览：自定义绘制水平进度条，按 CPU% 降序排列
- 栈配置对话框：手动录入各任务总栈大小，持久化到 QSettings
- 状态统计栏：显示上次更新时间、任务总数、各状态计数

## 新增实时波形曲线图 (Waveform Viewer)

新增一个完整的实时波形曲线图页面，用于可视化串口数据中的数值型数据。

### 新增文件
- `src/app/pages/WaveformPage.h/.cpp` — 页面容器：工具栏(添加序列/暂停/清空/时间窗口) + 图表控件
- `src/app/widgets/WaveformChartWidget.h/.cpp` — 核心图表控件：基于 QChartView，支持多序列、滚动时间窗口、正则解析

### 修改文件
- `src/app/AppShell.h/.cpp` — 注册 WaveformPage、侧边栏项、首页按钮连接
- `src/app/pages/HomePage.h/.cpp` — 添加波形图模块按钮和信号
- `src/app/widgets/ModuleIconFactory.cpp` — 添加 waveform 图标(正弦波曲线)
- `src/app/CMakeLists.txt` — 添加新源文件 + Qt6::Charts 链接
- `CMakeLists.txt` — 添加 Charts 到 find_package

### 功能特性
- 多序列叠加显示(最多8条，各色不同)
- 8种预设解析规则 + 自定义正则
- 滚动时间窗口(1-300秒，默认10秒)
- 暂停/继续/清空控制
- 自动 Y 轴范围调整(留10%边距)
- 通过 DataBus 订阅 transport.serial.* 通道获取实时串口数据

## 修复 est_app.exe 间歇性崩溃

### 背景
est_app.exe 启动时间歇性崩溃（约 62% 崩溃率），表现为启动后 2-55 秒内 "terminated abnormally"。

### 定位的根因
1. **QSS 引用不存在的资源** (`wood_classic.qss`, `industrial_dark.qss`) 中 `QCheckBox::indicator:checked` 引用了 `url(:/icons/check.png)`，该资源不在 QRC 中，可能导致渲染时不可预测行为
2. **SerialAssistantPage 构造时直接调用 QSerialPortInfo::availablePorts()** — Windows 串口枚举在驱动状态转换时可能阻塞或崩溃
3. **MemoryAnalyzerWidget 使用临时 RecentRecordManager 对象** — `loadElfFile()`, `loadMapFile()`, `refreshRecentFiles()` 三处使用 `RecentRecordManager(this)` 创建临时对象，导致不必要的文件 I/O
4. **Qt 6.11 缺少字体目录** — 日志显示 `QFontDatabase: Cannot find font directory`

### 修改的文件
- `src/resources/themes/wood_classic.qss` — 移除 `image: url(:/icons/check.png)`
- `src/resources/themes/industrial_dark.qss` — 同上
- `src/resources/qt.conf` — 新建文件，配置 Qt Prefix 路径
- `src/app/CMakeLists.txt` — 添加 qt.conf 复制到构建输出目录
- `src/app/pages/SerialAssistantPage.cpp` — 延迟 `refreshPorts()` 到事件循环就绪后
- `src/app/widgets/MemoryAnalyzerWidget.h` — 添加 `RecentRecordManager*` 成员和 setter
- `src/app/widgets/MemoryAnalyzerWidget.cpp` — 改用成员指针代替临时对象
- `src/app/pages/MemoryAnalyzerPage.h` — 添加 `setRecentRecordManager` 接口
- `src/app/pages/MemoryAnalyzerPage.cpp` — 实现接口并透传到 widget
- `src/app/appshell.cpp` — 为 MemoryAnalyzerPage 设置 RecentRecordManager

## 修复 DiffMinimapWidget 缩略图差异显示不全和拖动跳转

### Bug 1：差异显示不全
**原因**：`rebuildCache()` 使用 run-batching（按相同状态聚合色块），当文件行数远大于缩略图高度（hRatio < 1）时每个 run 只有 1 像素，差异块不可见。
**修复**：在 hRatio < 1.5 时切换为像素级差异优先绘制，逐像素行扫描对应源行范围，有差异则优先绘制差异颜色。

### Bug 2：不能拖动跳转
**原因**：只实现了 mousePressEvent（点击跳转），缺少拖动支持。
**修复**：添加 mouseMoveEvent（拖动中持续跳转）+ mouseReleaseEvent（停止拖动），使用 qBound 防越界。

### 修改的文件
- `src/app/widgets/DiffMinimapWidget.h` — 添加 mouseMoveEvent/mouseReleaseEvent/m_isDragging
- `src/app/widgets/DiffMinimapWidget.cpp` — 实现像素级差异优先绘制 + 拖动跳转

## 全量修复偶发性闪退问题（6项）

### P0-1: 串口枚举崩溃
- **问题**: `QSerialPortInfo::availablePorts()` 在 USB-串口驱动异常时触发 SEH 异常导致进程崩溃
- **修复**: 新建 `SerialPortEnumerator.cpp/h` 独立编译单元（/EHa），用 `__try/__except` SEH 保护；`SerialConfigBar::showPopup()` 改为延迟触发 `refreshRequested`
- **文件**: `pages/SerialPortEnumerator.cpp/h`(新), `pages/SerialAssistantPage.cpp`, `widgets/SerialConfigBar.cpp`, `CMakeLists.txt`

### P0-2: 高频串口接收QTextDocument损坏
- **问题**: `SerialReceiveView::appendPacket()` 手动 `cursor.removeSelectedText()` + `deleteChar()` 删除旧行，高频时损坏 QTextDocument 内部结构
- **修复**: 用 `QPlainTextEdit::setMaximumBlockCount(5000)` 替代手动删除
- **文件**: `widgets/SerialReceiveView.cpp`

### P1-1: 插件DLL加载崩溃
- **问题**: `QPluginLoader::instance()` 加载不兼容DLL直接崩溃，无元数据预检
- **修复**: 加载前先检查 `QPluginLoader::metaData()` 的 IID 和 Qt 版本号
- **文件**: `core/plugin/PluginLoader.cpp`

### P1-2: FileComparePage渲染竞态条件
- **问题**: `renderNextChunk()` 通过 QTimer 分块渲染时，`clearComparison()` 可能清空 pending 数据
- **修复**: `renderNextChunk()` 开头增加空数据防御检查，`m_pendingLineLists` 访问增加边界检查
- **文件**: `pages/FileComparePage.cpp`

### P1-3: RecentRecordManager并发JSON读写
- **问题**: 快速串口连接/断开时并发读写 JSON 文件
- **修复**: 为所有公共方法添加 `QMutexLocker` 保护
- **文件**: `core/services/RecentRecordManager.cpp/h`

### P2: QPushButton::setMenu双重释放
- **问题**: `setMenu(menu)` 后 `delete oldMenu`，但 setMenu 已接管所有权
- **修复**: 移除 `delete oldMenu`，改用 `Q_UNUSED(oldMenu)`
- **文件**: `pages/FileComparePage.cpp`

### 技术要点
- MSVC 的 `__try/__except` 不能在含需析构局部 C++ 对象的函数中使用（C2712），需隔离到无 C++ 栈对象的函数
- `/EHa` 编译选项允许异步异常但不豁免 C2712 限制
- `setMaximumBlockCount()` 是 QPlainTextEdit 内置的行数限制方案，比手动 cursor 操作安全

## 修复数据录制回放功能（回放数据不显示）

### 问题
录制功能正常，但回放时 SerialAssistantPage、SerialConsolePage、CANBusPage 页面空白，没有数据显示。

### 根因
这三个页面没有订阅 DataBus，回放数据通过 DataBus::publish() 发布但无订阅者接收。

### 修复
1. **SerialAssistantPage** — 构造函数中订阅 `transport.serial.*`，回调中过滤 `est.replayed` 只处理回放数据
2. **SerialConsolePage** — `setupTerminalTab()` 中启用 `m_terminal->setDataBus(m_dataBus)`，TerminalWidget 内部已有完整订阅逻辑
3. **CANBusPage** — 构造函数中订阅 `transport.can.*`，回调中过滤 `est.replayed`；同时补充了 `parseIncomingData()` 和 `sendCurrentFrame()` 中调用 `publishFrame()`，确保实时数据也能被录制

### 修改文件
- `src/app/pages/SerialAssistantPage.h` — 添加 `#include "databus/SubscriptionHandle.h"` 和 `SubscriptionHandle m_subscription`
- `src/app/pages/SerialAssistantPage.cpp` — 构造函数末尾添加 DataBus 订阅
- `src/app/pages/SerialConsolePage.cpp` — `setupTerminalTab()` 中启用 TerminalWidget DataBus 订阅
- `src/app/pages/CANBusPage.cpp` — 构造函数添加 DataBus 订阅，补充 publishFrame 调用

### 设计要点
- 实时数据通过 ITransport 直连显示（保持现有行为）
- 回放数据通过 DataBus 订阅显示（新增）
- 通过 `est.replayed` 元数据标记严格区分，避免实时模式下重复显示
