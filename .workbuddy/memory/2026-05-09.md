# 2026-05-09 Daily Log

## 清理项目目录
- 删除了所有构建产物目录：`build/` (1.5G)、`build-agent-check/` (893M)、`build-test-msvc/` (1.4G)、`dist/` (145M)、`build_temp/`、`build_mingw/`、`build_ninja/`、`build_msvc/`、`build-test/`
- 删除了重复的 `doc/` 目录（内容合并到 `docs/`）
- 删除了根目录多余文件：`nul`、`test_moc.cpp`、`widget-test-result.txt`、`package-build.log`
- 项目从约 4GB 缩减至 158MB

## 项目全面代码分析
- 对 src/app/、src/core/、src/resources/、CMakeLists.txt 进行了逐模块逐文件的详细代码审查
- 覆盖 26 个主要模块，识别出 40+ 个问题/优化点
- 编写完整分析报告 `docs/项目全面分析报告.md`
- 高严重程度问题 7 个（必须修复），中严重程度 15 个，低严重程度 18 个
- 关键问题包括：CANBusPage 重复 publish、ProtocolDecoder 数据截断计算错误、PluginLoader 悬空指针、industrial_dark.qss 主题内容错误、SerialConsolePage 临时文件竞争等

## 制定全量问题修复计划
- 基于分析报告的 40+ 问题，制定了分 4 阶段的修复计划 `docs/修复计划.md`
- Phase 1: 7 个高严重度关键 Bug（2-3天）
- Phase 2: 15 个中严重度功能缺陷（4-5天）
- Phase 3: 10 个低严重度代码质量提升（2-3天）
- Phase 4: 8 个架构优化长期改进（3-5天）
- 已创建 7 个 Phase 1 任务（Task #9-#15），等待用户确认后执行

## Phase 1 关键 Bug 修复（全部完成，构建通过）
1. **CANBusPage publishFrame 重复调用** — 删除 addFrameToTable() 末尾的冗余 publishFrame()
2. **ProtocolDecoderPage streamOffset 错误** — 新增 bufferOffset 参数修正偏移计算，更新 .h 声明
3. **ProtocolDecoder checksum remove() 偏移错位** — 两处均改为分段拼接跳过 checksum 区间（无帧头分支 + 有帧头分支）
4. **ProtocolDecoder frameSize=0 Payload 无法匹配** — 变长帧通过搜索下一帧头或尾部偏移推断帧长
5. **PluginLoader unloadAll 未注销插件** — 卸载前先从 PluginRegistry 注销所有插件
6. **industrial_dark.qss 暗色主题重写** — 完全重写为暗色工业配色方案
7. **SerialConsolePage Python 临时文件路径固定** — 改为 PID 后缀避免多实例竞争

构建环境：MSVC2022 (F:\Microsoft Visual Studio\18\Community) + Qt6.11.0 + JOM
构建目录：build/Desktop_Qt_6_11_0_MSVC2022_64bit-Release/
构建脚本：/tmp/build_est.bat（vcvarsall + jom PATH + cmake --build）

## 打包 EST Studio
- 清理产物目录中的旧 exe，创建 `EST_Studio_v2.0.0.zip`（约 50MB）
- 包含 est_app.exe + 所有 Qt DLL + 平台插件，解压即用
